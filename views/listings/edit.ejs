<!--<% layout("/layouts/boilerplate") %>
<body>
    <h1>Edit Listing</h1>
    <form method="POST" action="/listings/<%=listing._id%>?_method=PUT" >
        <input name="listing[title]" value="<%=listing.title%>" input type="text">
        <br><br>
        <textarea  name="listing[description]" ><%=listing.description%></textarea>
        <br><br>
        <input name="listing[image]" value="<%=listing.image.url%>"input type="text">
        <br><br>
        <input name="listing[price]" value="<%=listing.price%>" input type="number">
        <br><br>
        <input name="listing[country]" value="<%=listing.country%>" input type="text">
        <br><br>
        <input name="listing[location]" value="<%=listing.location%>" input type="text">
        <br><br>
        <button>Edit</button>
    </form>
</body>-->
<% layout("/layouts/boilerplate") %>

<div class="page-bg py-5">
  <div class="container">

    <!-- 🟣 Page Header (Same as new.ejs) -->
    <div class="text-center mb-5">
      <h1 class="fw-bold text-violet">Edit Listing</h1>
      <p class="text-muted fs-5">
        Make updates to your existing property details.
      </p>
    </div>

    <!-- 📝 Form (Same structure as new.ejs) -->
    <form method="POST" action="/listings/<%= listing._id %>?_method=PUT" novalidate class="needs-validation" enctype="multipart/form-data">

      <div class="row mb-4">
        <div class="col-md-6">
          <label class="form-label fw-semibold">Title</label>
          <input type="text" name="listing[title]" value="<%= listing.title %>" class="form-control" required>
        <div class="valid-feedback">Title looks good!</div>
        <div class="invalid-feedback">Title should be valid</div>
        </div>
        <div class="col-md-6">
          <label class="form-label fw-semibold">Price (₹/night)</label>
          <input type="number" name="listing[price]" value="<%= listing.price %>" class="form-control" required>
         <div class="invalid-feedback">please enter a valid price</div>
        </div>
      </div>

      <div class="row mb-4">
        <div class="col-md-6">
          <label class="form-label fw-semibold">Country</label>
          <input type="text" name="listing[country]" value="<%= listing.country %>" class="form-control" required>
         <div class="invalid-feedback">country name should be valid</div>
        </div>
        <div class="col-md-6">
          <label class="form-label fw-semibold">Location</label>
          <input type="text" name="listing[location]" value="<%= listing.location %>" class="form-control" required>
         <div class="invalid-feedback">Location should be valid</div>
        </div>
      </div>
      <div class="mb-4">
        <p>Original image</p>
        <img src="<%=originalImageUrl%>" style="width:300px; height:250px;"/>
      </div>

      <div class="row mb-4">
        <div class="col-12">
          <label class="form-label fw-semibold">Upload Listing Image</label>
          <input type="file" name="listing[image]" class="form-control">
        </div>
      </div>

      <div class="row mb-4">
        <div class="col-12">
          <label class="form-label fw-semibold">Description</label>
          <textarea name="listing[description]" class="form-control" rows="4" required><%= listing.description %></textarea>
        <div class="valid-feedback">perfect!</div>
        <div class="invalid-feedback">please enter a short description</div>
        </div>
      </div>

      <div class="text-center mt-4">
        <button type="submit" class=" btn-violet px-5 py-2">Save Changes</button>
      </div>

    </form>

  </div>
</div>
  
