<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Nestora | Welcome</title>
  <style>
    body, html {
      margin: 0;
      padding: 0;
      height: 100%;
      font-family: Arial, sans-serif;
    }

    /* Hero Section */
    .hero {
      height: 100vh;
      background: url("https://images.unsplash.com/photo-1507525428034-b723cf961d3e") no-repeat center center/cover;
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      color: white;
      text-align: center;
    }

    /* Dark overlay for readability */
    .hero::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.5);
    }

    .hero-content {
      position: relative;
      z-index: 1;
      max-width: 700px;
      padding: 20px;
    }

    .hero h1 {
      font-size: 3rem;
      margin-bottom: 20px;
      letter-spacing: 2px;
    }

    .hero p {
      font-size: 1.2rem;
      margin-bottom: 30px;
      line-height: 1.5;
    }

    .hero a {
      text-decoration: none;
      background: #7a7396;
      color: white;
      padding: 12px 25px;
      border-radius: 30px;
      font-size: 1rem;
      transition: background 0.3s ease;
    }

    .hero a:hover {
      background: #ff3d00;
    }

    /* Responsive text sizes */
    @media (max-width: 768px) {
      .hero h1 {
        font-size: 2rem;
      }
      .hero p {
        font-size: 1rem;
      }
    }
  </style>
</head>
<body>
  
  <!-- Welcome Hero Section -->
  <section class="hero">
    <div class="hero-content">
      <h1>Welcome to Nestora</h1>
      <p>Your gateway to unforgettable journeys and travel experiences.</p>
      <a href="/listings">Explore Now</a>
    </div>
  </section>

</body>
</html>

